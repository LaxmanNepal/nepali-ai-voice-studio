# Keep Swarlekha isolated from the other model environments.
torch>=2.6,<3
safetensors>=0.4
huggingface_hub>=0.25
# Install the upstream Swarlekha implementation that provides swarlekha_model.tts_nepali.
# Model checkpoints must remain in the Hugging Face cache, not Git.
