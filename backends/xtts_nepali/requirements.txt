# Isolated XTTS-v2 runtime.
# Keep this environment separate from other backends because Coqui TTS
# dependencies can conflict with other model stacks.

TTS>=0.22,<0.23
torch>=2.0,<3
torchaudio>=2.0,<3
huggingface_hub>=0.25
numpy>=1.26
